<Window x:Class="ImageEdit_WPF.AutoThreshold"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auto Threshold" Height="120" Width="300" Background="WhiteSmoke" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <WrapPanel Grid.Row="0">
            <TextBlock Margin="10">Distance between two peaks at histogram:</TextBlock>
            <TextBox Name="textboxDistance" Width="30" Height="25"></TextBox>
        </WrapPanel>
        <StackPanel Grid.Row="1" Margin="10" HorizontalAlignment="Center">
            <Button Name="ok" Padding="30,2,30,2" Click="ok_Click">OK</Button>
        </StackPanel>
    </Grid>
</Window>